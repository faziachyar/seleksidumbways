<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <title>
        <%= title %>
    </title>
</head>

<body>
    <div class="header">
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="#">
                <%= title %>
            </a>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="btn btn-sm btn-outline-primary" href="/addBlog"> Add image blog</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-sm btn-outline-primary" href="/addUser"> Add User</a>
                    </li>
                </ul>
            </div>
        </nav>


    </div>
    <div class="container">
        <% image_blog.forEach (function(user) { %>
            <div class="card" style="max-width: 500px;">
                <div class="row no-gutters">
                    <div class="col-md-4">
                        <img src="<%= user.file_image %>" class="card-img" alt="...">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= user.title %>
                            </h5>
                            <p class="card-text text-primary">
                                <%= user.name %>
                            </p>
                            <p class="card-text"><small class="text-muted"><%= user.content %></small></p>
                            <button type="button" class="btn btn-outline-info">view more</button>
                        </div>
                    </div>
                </div>
            </div>
            <br>
            <% }); %>
</body>

</html>